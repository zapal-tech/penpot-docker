# Self-hosted Penpot Docker configuration

This repository contains a docker compose file to run Penpot in a self-hosted environment.

You need to set your own environment variables in the `.env` file.
